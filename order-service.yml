server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/orderdb
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:root}
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
  application:
    name: ORDER-SERVICE
  config:
    import: configserver:${SERVER_URL:http://localhost:9296}
  security:
    oauth2:
      client:
        registration:
          internal-client:
            client-id: G9qRO7X7LQuyD1vs3oZOrajFxrvdYdaR
            client-secret: tfSmC_KvstHat25zZYtAEC4yVfWkV7OP5KC53bXw8nnGJJvoo3ddY1Rxnqx-e8ws
            scope: internal
            authorization-grant-type: client_credentials  # ✅ Ensure it's `authorization_code`
            redirect-uri: "{baseUrl}/login/oauth2/code/auth0"
            #client-authentication-method: client_secret_basic
            provider: internal-provider
        provider:
          internal-provider:
            issuer-uri: https://dev-sb27fsto2d2kckct.us.auth0.com/
            jwk-set-uri: https://dev-sb27fsto2d2kckct.us.auth0.com/.well-known/jwks.json
      resourceserver:
        jwt:
          issuer-uri: https://dev-sb27fsto2d2kckct.us.auth0.com/
  aop:
    proxy-target-class: true
  zipkin:
    base-url: ${ZIPKIN_URL:http://localhost:9411}
    enabled: true
management:
  tracing:
    sampling:
      probability: 1.0
microservice:
  product: http://PRODUCT-SERVICE
  payment: http://PAYMENT-SERVICE
  
resilience4j:
  circuitbreaker:
    instances:
      external:
        event-consumer-buffer-size: 10
        failure-rate-threshold: 50
        minimum-number-of-calls: 5
        automatic-transition-from-open-to-half-open-enabled: true
        wait-duration-in-open-state: 5s
        permitted-number-of-calls-in-half-open-state: 3
        sliding-window-size: 10
        sliding-window-type: COUNT_BASED

#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka}
